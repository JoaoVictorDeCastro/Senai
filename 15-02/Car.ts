class Car {
    public Boolean;

    constructor(auto) {
        this.auto = false;
    }

    public estado(auto){
      if (auto == true) {
        console.log("O carro está ligando...");
        console.log("O carro está ligado!");
      }
      if (auto == false) {
        console.log("O carro está desligado!")
      }
    }
  }

var car = new Car();
this.auto = true;
car.estado;
